//  quoting string with single quotes

const doubleQuoteStr = "This is a string"; 
const singleQuoteStr = 'This is also a string';

const conversation = 'Finn exclaims to Jake, "Algebraic!"';

const goodStr = 'Jake asks Finn, "Hey, let\'s go on an adventure?"';

const sampleStr = 'Alan said, "Let\'s learn JavaScript".';

const myStr = '<a href="http://www.example.com" target="_blank">Link</a>';

//  put string inside single quote for using double quote in string


const testStr = 'I am a "double quoted" string inside \'single quotes\'.';

//  for single quote in string \' used at start and end